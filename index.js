const { ArrayBehaviors, ObjectBehaviors, TRANSLATION_DEFAULTS } = require('./src/constants');
const { translateObject } = require('./src');
const { _utils } = require('./src/utils');

module.exports = {
  ArrayBehaviors,
  ObjectBehaviors,
  TRANSLATION_DEFAULTS,
  translateObject,
  _utils,
};
